// prisma/schema.prisma

datasource db {
  provider = "postgresql"
  // main url
  // url      = env("DATABASE_URL")
  //test url
  url = env("DATABASE_AUTH_BRANCH_URL")
}

generator client {
  provider = "prisma-client-js"
}